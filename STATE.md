# hypergraph-protocol — State

> Generated by `tools/hypergraph.py render` from the state-graph export.
> Do not hand-edit — run the hypergraph-reconcile skill instead.

Reconciled through `patient-ridge-8464`, `lean-field-0101`, `careful-oak-1768`, `loyal-tide-3608` at 2026-08-16T18:55:00+00:00.

## Frontier

- [open] **Announcement** (`weathered-union-7494`) — The one unshipped half of distribution: a **spec-first announcement**, whose venue and wording are the Operator's call [rec: damp-mountai…
- [open] **Autonomous operation** (`hollow-rain-8997`) — **The claim the project leads with, and the one no state node made until now.** SPEC.md, README.md and AGENTS.md all open with "a substra…
- [open] **Protocol benchmark** (`protocol-benchmark-4417`) — **The gap this closes.** Everything to date shows the protocol works *mechanically* — `check` reports 0 violations on several repos, and …
- [open] **State graph** (`soft-hill-6082`) — The distilled half: what is true now, including the frontier of open, broken and blocked work. **This is the live hypothesis of the proje…
- [open] **Visualization** (`polished-pond-2718`) — The in-core visualizer is removed at 0.0.9. The viz section, the ~3,700-line embedded page template, the page sources, the bundler, the v…

## Architecture

- **hypergraph-protocol — state** (`cool-king-8586`)
  - [working] **Graph structure** (`young-wave-9364`) — SPEC.md is where the structure is written down: invariants I1–I8 — record-first, impact declaration, single-writer state, provenance, hig…
    - [working] **Record graph** (`wandering-crane-1698`) — The append-only half: everything that happened, one markdown file per node, causally parented [rec: spring-pine-7256]. It is the **mature…
    - [open] **State graph** (`soft-hill-6082`) — The distilled half: what is true now, including the frontier of open, broken and blocked work. **This is the live hypothesis of the proje…
  - [working] **Protocol mechanics** (`wandering-sun-8831`) — `tools/hypergraph.py` — a single-file uv script depending only on pyyaml — is the whole implementation. It is the mechanism half of the p…
    - [working] **Storage & node format** (`blue-sun-8921`) — What a node *is* on disk, and what a store would have to satisfy to hold one instead [rec: crimson-dawn-7137] [rec: old-dawn-8747].
    - [working] **Retroactive repair** (`retroactive-repair-5104`) — `hypergraph heal` carries a capability **backwards** into a repo that adopted before the capability existed. It is a registry of typed gr…
  - [working] **Skills** (`dry-wildflower-2260`) — **Five skills**: `hypergraph-init` (roots, skeleton, config), `hypergraph-record` (causally-parented record nodes, always declaring State…
  - [working] **Dogfooding** (`bold-field-1268`) — Dogfooding is this project's evidence that the protocol holds when it is used rather than described, and it has two genuinely separate ba…
    - [working] **Field** (`bitter-sound-9744`) — Repos that adopted the protocol, three of which this project did not write. This is where defects that survive the author's assumptions a…
    - [working] **Self-host** (`sage-river-8211`) — This repo running under its own protocol — the first evidence base, and the one with no escape hatch, since a defect here is a defect in …
  - [open] **Visualization** (`polished-pond-2718`) — The in-core visualizer is removed at 0.0.9. The viz section, the ~3,700-line embedded page template, the page sources, the bundler, the v…
    - [superseded] **Viz machinery** (`brave-bramble-9399`) — **Superseded**: the machinery this node describes was removed from the repo at 0.0.9 — template, bundler, sources, browser baselines and …
    - [superseded] **Views** (`lawful-ash-6222`) — **Superseded**: the page these views lived on was removed from the repo at 0.0.9 with the viz cut; the seam that replaces it is described…
  - [working] **Flywheel mirror** (`empty-forest-6305`) — An optional, one-way projection of the committed node files onto a hosted graph. The repo stays canonical; nothing here is ever read back…
  - [working] **Adoption** (`morning-crane-7863`) — Bringing a repo that already has a past under the protocol. Built, shipped, and field-proven end to end in both modes [rec: vast-sky-3964].
    - [working] **Upgrade path** (`fond-sail-3288`) — How a release reaches a project that already adopted the protocol. Three things travel and they update by three different mechanisms, whi…
  - [open] **Protocol benchmark** (`protocol-benchmark-4417`) — **The gap this closes.** Everything to date shows the protocol works *mechanically* — `check` reports 0 violations on several repos, and …
  - [working] **Collaboration** (`gilded-vale-8087`) — **The settled framing: the record/state split already *is* the fork/merge split**, and it was not designed for that. The record graph is …
  - [working] **Distribution** (`damp-basin-8974`) — How the protocol reaches anyone who is not in this checkout. The shape was decided once and has held [rec: damp-mountain-8757]: **spec-fi…
    - [open] **Announcement** (`weathered-union-7494`) — The one unshipped half of distribution: a **spec-first announcement**, whose venue and wording are the Operator's call [rec: damp-mountai…
    - [working] **PyPI releases** (`odd-birch-3808`) — Five releases are live on PyPI, and every one was verified **from the public index** rather than from `dist/` — the check that distinguis…
    - [working] **GitHub repository** (`still-tide-5192`) — The repository is **public**, at `theo-kirby/hypergraph-protocol` after a rename from `hypergraph` that left the old URL redirecting [rec…
  - [open] **Autonomous operation** (`hollow-rain-8997`) — **The claim the project leads with, and the one no state node made until now.** SPEC.md, README.md and AGENTS.md all open with "a substra…
    - [working] **Harness hygiene** (`fair-field-3265`) — What this project has learned about **running fleets of autonomous agents against live accounts** — the operational discipline *for* auto…
