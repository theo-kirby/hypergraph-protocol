# hypergraph-protocol — State

> Generated by `tools/hypergraph.py render` from the state-graph export.
> Do not hand-edit — run the hypergraph-reconcile skill instead.

Reconciled through `humble-rain-0304` at 2026-08-09T16:30:00+00:00.

## Frontier

- [open] **Protocol benchmark** (`protocol-benchmark-4417`) — **The gap this closes.** Everything to date shows the protocol works *mechanically* — `check` reports 0 violations on three repos, and a …
- [open] **Publication** (`weathered-union-7494`) — Publication shape decided [rec: damp-mountain-8757]: spec-first (SPEC.md is the durable artifact), a PyPI CLI package as the distribution…

## Architecture

- **hypergraph-protocol — state** (`cool-king-8586`)
  - [working] **Protocol spec** (`young-wave-9364`) — SPEC.md defines the protocol: invariants I1–I8 (record-first, impact declaration, single-writer state, provenance, high-water mark, statu…
  - [working] **Storage interface** (`blue-sun-8921`) — backend/INTERFACE.md defines the 10 abstract operations the protocol is written against (create_root, append_record_node, read_node, list…
  - [working] **Checker tooling** (`wandering-sun-8831`) — tools/hypergraph.py (single-file uv script, pyyaml only) implements `check` — mechanical validation of I2, I4, I5, I6, I7 over offline JS…
  - [working] **Skills** (`dry-wildflower-2260`) — Five skills: hypergraph-init (roots + skeleton + config), hypergraph-record (causally-parented record nodes, always declares State Impact…
  - [working] **Dogfooding** (`bold-field-1268`) — Full self-host cycle completed on this repo: live init, retro-recorded design + M1–M4, reconcile pass over four state nodes + HWM advance…
  - [working] **Visualization** (`polished-pond-2718`) — `tools/hypergraph.py viz` emits a self-contained interactive HTML page with four views named after the job each does: **Timeline** (the r…
  - [working] **Storage and the optional mirror** (`empty-forest-6305`) — The git-native storage is the *only* storage: markdown node files under `.hypergraph/graph/{record,state}/<slug>.md` are the source of tr…
  - [working] **Field dogfooding** (`bitter-sound-9744`) — Both dogfooding targets are live under the protocol [rec: vast-sky-3964]: **a3go** adopted mode A — 108-node legacy Flywheel graph import…
  - [open] **Publication** (`weathered-union-7494`) — Publication shape decided [rec: damp-mountain-8757]: spec-first (SPEC.md is the durable artifact), a PyPI CLI package as the distribution…
  - [working] **Adoption** (`morning-crane-7863`) — The adoption path is built, shipped, and field-proven end-to-end. Settled design held throughout: all-local import default, epoch-split o…
  - [open] **Protocol benchmark** (`protocol-benchmark-4417`) — **The gap this closes.** Everything to date shows the protocol works *mechanically* — `check` reports 0 violations on three repos, and a …
  - [working] **Harness hygiene** (`fair-field-3265`) — What this project has learned about **running fleets of autonomous agents against live accounts** — harness discipline, as distinct from …
  - [working] **Collaboration** (`gilded-vale-8087`) — **The settled framing: the record/state split already *is* the fork/merge split**, and it was not designed for that. The record graph is …
  - [working] **Upgrade path** (`fond-sail-3288`) — How a release reaches a project that already adopted the protocol. Three things
